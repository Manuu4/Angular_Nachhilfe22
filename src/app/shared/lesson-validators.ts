import {NachhilfeService} from "./nachhilfe.service";
import {FormControl} from "@angular/forms";
import {map, Observable} from "rxjs";

export class LessonValidators {

  // static titleExists(bs : NachhilfeService) {
  //   return function(control: FormControl) : Observable<any> {
  //     return bs.check(control.value).pipe(map(exists => !exists ? null : {titleExists: {valid:false}}));
  //   }
  // }

}

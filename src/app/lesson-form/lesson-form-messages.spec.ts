import { LessonFormMessages } from './lesson-form-messages';

describe('LessonFormMessages', () => {
  it('should create an instance', () => {
    expect(new LessonFormMessages()).toBeTruthy();
  });
});
